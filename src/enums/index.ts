export enum TUTORS {
  ENROLLED_TUTORS = "enrolledTutors",
  PENDING_REQUESTS = "pendingRequests",
  REJECTED_TUTORS = "rejectedTutors",
  DEACTIVATED_TUTORS = "deactivatedTutors",
}

export enum STUDENTS {
  ENROLLED_STUDENTS = "enrolledStudents",
  PENDING_REQUESTS = "pendingRequests",
  REJECTED_STUDENTS = "rejectedStudents",
  DEACTIVATED_STUDENTS = "deactivatedStudents",
}

export enum TESTIMONIALS {
  ADMIN = "admin",
  STUDENTS = "students",
  TUTORS = "tutors",
  REQUESTS = "requests",
}

export enum FAQS {
  FAQSFORSTUDENTS = "FaqForStudents",
  FAQSFORTUTORS = "FaqForTutors",
}
